name: predictive_maintenance_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - mlflow==2.11.3
      - pandas
      - numpy
      - scikit-learn
      - dagshub
      - matplotlib
      - seaborn